import React from 'react';
import Log from "../components/Log";

const CreateProject = () => {
    return (
        <div>
            page add project
        </div>
    );
};

export default CreateProject;